<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>TunAcademy</title>
    <meta name="keywords" content="" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <link
      rel="shortcut icon"
      href="images/logo.png"
      type="image/x-icon"
    />
    <link rel="apple-touch-icon" href="#" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="css/bootstrap.min.css" />
    <!-- Pogo Slider CSS -->
    <link rel="stylesheet" href="css/pogo-slider.min.css" />
    <!-- Site CSS -->
    <link rel="stylesheet" href="css/style.css" />
    <!-- Responsive CSS -->
    <link rel="stylesheet" href="css/responsive.css" />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="css/custom.css" />
    <link rel="stylesheet" href="css/reaserch.css" />
    <style>


/* Global */
body {
  font-family: 'Poppins', sans-serif;
  background-color: #f9f9f9;
  color: #333;
}

/* Header */
.top-header {
  background-color: #ffffff;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
}

/* Navbar Links */
.navbar-nav .nav-link {
  font-weight: 500;
  color: #2c7a7b;
  transition: 0.3s;
}
.navbar-nav .nav-link:hover {
  color: #48bb78;
}

/* Upload Section */
#upload {
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding-top: 180px;
  background: linear-gradient(to right, #e0f2f1, #f9f9f9);
}

#upload h2 {
  font-weight: 700;
  color: #2c7a7b;
}

#upload p {
  font-size: 18px;
  color: #666;
  margin-bottom: 30px;
}

#upload form {
  background: white;
  border-radius: 12px;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
  padding: 30px;
}

#upload .form-control {
  border-radius: 8px;
  padding: 12px 15px;
  font-size: 16px;
}

#upload .btn-success {
  background-color: #2c7a7b;
  border: none;
  font-size: 18px;
  padding: 12px;
  border-radius: 8px;
}
#upload .btn-success:hover {
  background-color: #48bb78;
}

/* Footer */
.footer-box {
  background-color: #2c7a7b;
  padding-top: 50px;
  padding-bottom: 30px;
  color: #ffffff;
  font-size: 15px;
}

.footer_blog h5, .footer_blog h3 {
  font-weight: 700;
  margin-bottom: 20px;
}

.footer_blog ul li {
  margin-bottom: 10px;
}

.footer_blog ul li a {
  color: #d4f4ec;
  text-decoration: none;
  transition: 0.3s;
}

.footer_blog ul li a:hover {
  text-decoration: underline;
  color: #ffffff;
}

/* Responsive */
@media (max-width: 768px) {
  #upload {
    padding-top: 100px;
  }
  #upload form {
    padding: 20px;
  }
}

</style>

    </style>
    <link
      rel="shortcut icon"
      href="images/footer_logo.png"
      type="image/x-icon"
    />

    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="css/custom.css" />
  </head>
  <body>
    <!-- Start header -->
    <header class="top-header">
      <nav class="navbar header-nav navbar-expand-lg">
        <div class="container-fluid">
          <a class="navbar-brand" href="/"
            ><img style="width: 220px" src="images/logo.png" alt="image"
          /></a>
          <button
            class="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbar-wd"
            aria-controls="navbar-wd"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span></span>
            <span></span>
            <span></span>
          </button>
          <div
            class="collapse navbar-collapse justify-content-end"
            id="navbar-wd"
          >
            <ul class="navbar-nav">
              <li><a class="nav-link active" href="/">Acceuil</a></li>
              <li><a class="nav-link" href="/pfe">Upload</a></li>
              <% if (loggedIn) { %>
              <li><a class="nav-link" href="/profile">My Account</a></li>
              <% } else { %>
              <li><a class="nav-link" href="/login">Login</a></li>
              <% } %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <!-- Upload Section -->
    <section id="upload" class="py-5" style="padding-top: 180px">
      <div class="container text-center">
        <h2 class="mb-4">Upload Your Waste Image</h2>
        <p class="mb-5">
          Use our AI engine to determine if your item is recyclable. Simple.
          Fast. Eco-friendly.
        </p>
        <div class="row">
          <div class="col-md-8 col-lg-6 mx-auto">
            <form
              action="/upload"
              method="POST"
              enctype="multipart/form-data"
              class="p-4 border rounded bg-light shadow"
            >
              <div class="mb-3">
                <input
                  type="file"
                  class="form-control"
                  name="image"
                  accept="image/*"
                  required
                />
              </div>
              <button type="submit" class="btn btn-success btn-lg w-100">
                Scan Now
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <!-- Start Footer -->
    <footer
      class="footer-box"
      style="background-color: #2c7a7b; color: white; padding: 60px 0"
    >
      <div class="container">
        <div class="row">
          <!-- Logo + Mission -->
          <div class="col-md-4 mb-4">
            <div class="footer_blog">
              <div class="full mb-3"></div>
              <p style="font-size: 15px">
                Trash to Cash helps you recycle smarter! Upload your waste photo
                and find out instantly if it’s recyclable. Join the movement to
                protect our planet, one scan at a time. 🌍♻️
              </p>
            </div>
          </div>

          <!-- Quick Links -->
          <div class="col-md-4 mb-4">
            <div class="footer_blog footer_menu">
              <h5 class="mb-4">Quick Links</h5>
              <ul style="list-style: none; padding-left: 0">
                <li><a href="/" style="color: #d4f4ec">Home</a></li>
                <li><a href="#about" style="color: #d4f4ec">About</a></li>
                <li><a href="#upload" style="color: #d4f4ec">Upload</a></li>
                <li><a href="#features" style="color: #d4f4ec">Features</a></li>
                <li><a href="#contact" style="color: #d4f4ec">Contact</a></li>
              </ul>
            </div>
          </div>

          <!-- Contact Info -->
          <div class="col-md-4 mb-4">
            <div class="footer_blog">
              <h5 class="mb-4">Get in Touch</h5>
              <ul style="list-style: none; padding-left: 0">
                <li><i class="fa fa-map-marker"></i> &nbsp;Nabeul, Tunisia</li>
                <li>
                  <i class="fa fa-envelope"></i> &nbsp;support@trashtocash.com
                </li>
                <li><i class="fa fa-phone"></i> &nbsp;+216 29 722 048</li>
              </ul>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-12 text-center mt-4">
            <p style="font-size: 14px">
              © 2025 Trash to Cash. All rights reserved.
            </p>
          </div>
        </div>
      </div>
    </footer>
    <!-- End Footer -->
    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Your existing scripts -->
    <script>
      const universitySelect = document.getElementById("university");
      const instituteSelect = document.getElementById("institute");

      // Fetch and populate universities
      async function loadUniversities() {
        try {
          const response = await fetch("/universities");
          const universities = await response.json();
          universitySelect.innerHTML =
            '<option value="" disabled selected>Select University</option>';
          universities.forEach((university) => {
            const option = document.createElement("option");
            option.value = university.name;
            option.textContent = university.name;
            universitySelect.appendChild(option);
          });
        } catch (error) {
          console.error("Error loading universities:", error);
        }
      }

      // Fetch and populate institutes based on selected university
      async function updateInstitutes() {
        const selectedUniversity = universitySelect.value;
        if (!selectedUniversity) {
          instituteSelect.innerHTML =
            '<option value="" disabled selected>Select Institute</option>';
          return;
        }

        try {
          const response = await fetch(
            `/institutes/${encodeURIComponent(selectedUniversity)}`
          );
          const institutes = await response.json();
          instituteSelect.innerHTML =
            '<option value="" disabled selected>Select Institute</option>';
          institutes.forEach((institute) => {
            const option = document.createElement("option");
            option.value = institute.name;
            option.textContent = institute.name;
            instituteSelect.appendChild(option);
          });
        } catch (error) {
          console.error("Error loading institutes:", error);
        }
      }

      // Load universities on page load
      window.addEventListener("DOMContentLoaded", loadUniversities);

      // Update institutes when a university is selected
      universitySelect.addEventListener("change", updateInstitutes);

      function saveOptions() {
        const option2 = document.getElementById("institute").value;
        const option3 = document.getElementById("university niveau").value;
        localStorage.setItem("option2", option2);
        localStorage.setItem("option3", option3);
        // Redirect to /cards only if both options are filled
        window.location.href = "/cards";
      }
    </script>
    <script>
      document
        .getElementById("pfeForm")
        .addEventListener("submit", async function (e) {
          e.preventDefault();

          const formData = new FormData(this);
          const data = Object.fromEntries(formData.entries());

          try {
            const response = await fetch("/PFE", {
              method: "POST",
              headers: {
                "Content-Type": "application/json",
              },
              body: JSON.stringify(data),
            });

            const result = await response.json();

            if (response.status === 401 && result.redirect) {
              window.location.href = "/login"; // force page redirect
              return;
            }

            if (response.ok) {
              alert(result.message); // success
              this.reset();
            } else {
              alert(result.message); // error from backend
            }
          } catch (error) {
            alert("Une erreur réseau s'est produite. Veuillez réessayer.");
          }
        });
    </script>
  </body>
</html>
